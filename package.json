{
  "name": "check-branch-name-action",
  "version": "1.0.0",
  "description": "Checks the branch name for correct jira issue key.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/github": "^2.2.0"
  }
}
